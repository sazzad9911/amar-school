import React from 'react'
import { View } from 'react-native'

export default function Curriculum({navigation,route}) {
  const params=route.params;
  //console.log(params.data)
  return (
   <View>

   </View>
  )
}
